<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">Achievements</h1>

  <div class="grid gap-6 md:grid-cols-2">
    <% @sets.each do |set| %>
      <%= render 'set_card', set: set %>
    <% end %>
  </div>

  <p class="text-xs opacity-60 mt-8">Region detection is based on approximate boundaries.</p>
</div>
